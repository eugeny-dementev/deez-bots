# This file is generated by Nx.
#
# Build the docker image with `nx docker-build cameras`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with
# docker run -d --restart=always `
# -v path/to/tmp:/tmp `
# -v path/to/videos:/videos `
# -v path/to/cookies.txt:/cookies.txt `
# --env-file=./bots/cameras/.production.env `
# --name=cameras-bot cameras-bot-image
FROM utils/dl

WORKDIR /app

RUN addgroup --system cameras && \
    adduser --system -G cameras cameras

COPY dist/bots/cameras cameras
RUN chown -R cameras:cameras .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix cameras --omit=dev -f install

CMD [ "node", "cameras" ]
