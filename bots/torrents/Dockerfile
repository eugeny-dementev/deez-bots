# This file is generated by Nx.
#
# Build the docker image with `nx docker-build torrents`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with
# docker run -d --restart=always `
# -v path/to/tmp:/tmp `
# -v path/to/videos:/videos `
# -v path/to/cookies.txt:/cookies.txt `
# --env-file=./bots/torrents/.production.env `
# --name=torrents-bot torrents-bot-image
FROM utils/mkv

WORKDIR /app

COPY dist/bots/torrents torrents

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix torrents --omit=dev -f install

CMD [ "node", "torrents" ]
